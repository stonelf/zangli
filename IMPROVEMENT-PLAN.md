# 百年藏历（zangli）改进计划 · v2

> 修订时间：2026-09-09
> 本版基于**老电脑备份恢复后的真实代码**重新评估，推翻了 v1 中的若干结论。
> 所有缺陷均标注了实测证据与代码位置，未实测的推测已单独标记。

---

## 0. 相比 v1 的重大修正

v1 是在「GitHub 上的 2019 年旧版小程序」上做的审计。备份恢复后（`wxapp/` 已补齐为 2021 年本地版本），结论有四处反转：

| v1 结论 | v2 实测结果 |
|---|---|
| 小程序副本缺整个「铁虎年」、1951 正月 `-28` | ❌ **错**。备份版 101 年、1951 正月 `-27`，与主库一致 |
| 小程序副本有日月食「双重 +8h 偏移」 | ❌ **错**。备份版已无双重偏移；但**主库和小程序都还有另一个时区问题**（见 P0-1） |
| 小程序副本 2050 年第 12 月缺 `[-8,12]` | ✅ 仍成立，但被 P0-2 的范围截断掩盖，修了 P0-2 才会暴露 |
| 主库是「最新最全」的一份 | ⚠️ **部分错**。小程序备份版在主库之前就修好了**半天边界**和**食甚丢失**两个 bug；主库反而还没修 |

**一句话**：主库和小程序各自演化，各自修了一半的 bug，谁都不完整。这正是「统一数据源 + 自动化测试」必须做的原因。

---

## 1. 当前现状

### 1.1 仓库结构（本次恢复后）

```
zangli/
├── zangli.js                 # 主库（网站用，最后提交 2022-04-14）
├── eclipse.js                # 又内联了一份 eclipseList（重复）
├── index.html                # 网站首页
├── calendar/1952..2050.html  # 99 个静态年历页，9.3 MB，缺 1951
└── wxapp/                    # ← 微信开发者工具的项目根目录
    ├── project.config.json   # miniprogramRoot: "zangli/", cloudfunctionRoot: "cloudfunctions/"
    ├── cloudfunctions/       # 6 个云函数源码（已排除 node_modules，原 190 MB）
    └── zangli/               # ← 小程序代码根目录（app.json 在此）
        ├── app.js / app.json / app.wxss / sitemap.json
        ├── pages/index/            # 月历（已注册）
        ├── pages/testWXgateway/    # 测试网关（已注册，且是启动页）
        ├── pages/connect/          # 未注册
        ├── pages/page_module_sign_up/  # 未注册（云开发签到模块）
        ├── lib/                    # sax/dom 解析
        └── style/guide.wxss
```

> 导入微信开发者工具时选 **`wxapp/`**（不是 `wxapp/zangli`，虽然那个也能跑）。

### 1.2 三份核心数据的实测对比

| 检查项 | 主库 `zangli.js` | 小程序（备份版） | v2 `utils/zangli.js` |
|---|---|---|---|
| 缺日/闰日表年数 | 101 | 101 | 101 |
| 与主库逐项 diff | — | **1 条差异**（2050 年第 12 月 `[-8,12]` 缺失） | **0 条** |
| `eclipseList` 条数 | 495 | 495（内容完全一致） | 495 |
| 半天边界（中午入参） | ❌ 偏一天（初二） | ✅ 正确（初一） | ✅ 正确 |
| 食甚显示 | ❌ 丢进 `extraInfo2`，网站不渲染 | ✅ 正常显示 | ✅ 正常 |
| 日月食时区 | ❌ 非东八区查不到 | ❌ 同上 | ✅ 四时区一致 |
| 可查日期上限 | 2051-02-10 | **2051-01-11**（少 30 天） | 2051-02-11 |
| 全量往返一致性 | 未测（无反向转换） | 无反向转换 | ✅ 36560 天 0 误差 |

---

## 2. 缺陷清单（按严重度）

### P0 — 结果错误，必须修

**P0-1　日月食日期键依赖本地时区（主库 + 小程序都有）**
- 位置：`zangli.js:259` `var d=new Date(eclipseList[i][0])` 之后按本地时区取日期；小程序同逻辑。
- 证据：`TZ=UTC` 和 `TZ=America/New_York` 下 `getEclipse(2025-09-08)` 返回**空**。数据是北京时间，但日期键用本地时区 → 海外用户打开 zangli.pro 查不到日月食。
- 修法：日期键固定按 UTC+8 计算，例如
  ```js
  var t = new Date(eclipseList[i][0] + 8 * 3600 * 1000);
  var key = t.getUTCFullYear() + "/" + (t.getUTCMonth() + 1) + "/" + t.getUTCDate();
  ```

**P0-2　小程序可查范围比主库少 30 天**
- 位置：`wxapp/zangli/pages/index/index.js:73`（`previousMonth` 硬编码 `1951/2/1`）和 `:80`（`nextMonth` 硬编码 `2051/1/12`），与同文件 `:290-291` 的 `startDate=1951/1/8` / `endDate=2051/2/11` 自相矛盾。
- 证据：主库 2051-01-12 ~ 2051-02-10 能算出「铁马年十二月初一…三十」，小程序返回 `undefined`。
- 修法：两处硬编码改成引用 `startDate` / `endDate`。

**P0-3　主库半天边界**
- 位置：`zangli.js` `var days=Math.round((d-startDate)/86400/1000)`。
- 证据：入参 `2026-02-18T12:00` → 主库「初二」，小程序「初一」。
- 修法：入口处规范化 `d = new Date(d.getFullYear(), d.getMonth(), d.getDate())`。

**P0-4　主库丢失「食甚」**
- 位置：`getEclipse` 在有初亏/复圆数据时把食甚塞进 `extraInfo2`，而 `index.html` 只渲染 `extraInfo`。
- 证据：2025-09-08 主库显示「初亏0点26分，复圆3点56分」，小程序显示「初亏0点26分，食甚2点12分，复圆3点56分」。
- 修法：把 `extraInfo2` 拼进 `extraInfo`（或让 `index.html` 一并渲染）。

**P0-5　小程序缺 2050 年（藏历铁马年）十二月 `[-8,12]`**
- 证据：主库 2051-01-19 显示「初九」（缺初八）、2051-01-23 显示「闰十二」；小程序表里这一项不存在。
- 注意：目前被 P0-2 的截断掩盖，**修完 P0-2 才会暴露**。修 P0-2 时必须同时补这条数据。

### P1 — 可用性 / 工程卫生

| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| P1-1 | `search()` 是死链 | `index.wxml` `bindlongpress='search'` → `index.js:88` `wx.navigateTo('../search/search')` | `pages/search` **不存在**，长按必报错 |
| P1-2 | 启动页是测试页 | `app.json` pages[0] = `pages/testWXgateway/index` | 打开先进测试网关，不是日历 |
| P1-3 | 硬编码云环境 | `app.js` `wx.cloud.init({ env: 'test-943705' })` | 云开发已收费（≈19.9 元/月起），而本程序全部计算在本地，**不需要云** |
| P1-4 | 未注册页面 | `pages/connect`、`pages/page_module_sign_up` | 不在 `app.json` 里，实际不可达（`connect` 依赖场景 1037 跳转） |
| P1-5 | 主库隐式全局 | `zangli.js` `getEclipse` 内 `var d`、`extraInfo`、`extraInfo2` | 严格模式 / ESM 下直接报错；本次实测已触发 `Identifier 'd' has already been declared` |
| P1-6 | 注释与代码矛盾 | `zangli.js:5`「2051年1月12日」、`:13`「从铁兔年一月初一（1951.2.7）开始」 | 代码实为 1951/1/8 ~ 2051/2/11；另 `:147` 注释「按秒转换」实为毫秒 |
| P1-7 | 代码噪音 | `wx.reportMonitor('1',1)`、`checkLine()` 死代码、wxss 用 `rem` | 小程序规范单位是 `rpx`；手写 touch 切月无惯性/回弹 |

### P2 — 工程化与长期

1. **四份数据拷贝**：主库 `zangli.js`、小程序 `index.js`、v2 `utils/zangli.js`、`eclipse.js`（又内联一份 `eclipseList`）。
2. **零测试、零 CI**：数据型项目最危险的地方——数据错误不会抛异常。
3. **网站**：`calendar/` 99 个静态页 9.3 MB、缺 1951 年、无生成脚本；无 PWA。
4. **未发 npm 包、无 TypeScript 类型声明**。
5. **合规**：ICP 备案 + 用户隐私保护指引（见 §4，强制项）。
6. **2051 年数据耗尽**：需引入 `date-tibetan` 做算法推算与历书交叉校验。
7. **日月食数据**：495 条中仅 77 条（15.6%）含初亏/复圆时刻，可用 NASA Five Millennium Canon 扩充。

---

## 3. 分阶段计划

### 期一 · 止血（1–3 天，不动目录结构）

目标：**先让线上结果变正确**，再改结构。建议与结构改动分两次提交，便于定位回归。

- [ ] P0-1 日月食日期键固定 UTC+8（`zangli.js` + 小程序各一处）
- [ ] P0-2 小程序上下限改引用 `startDate`/`endDate`
- [ ] P0-3 主库入口规范化到当日零点
- [ ] P0-4 食甚拼回 `extraInfo`
- [ ] P0-5 补 2050 年第 12 月 `[-8,12]`（**须与 P0-2 同一提交**）
- [ ] P1-1 删掉 `bindlongpress='search'` 与 `search()`（或补一个真页面）
- [ ] P1-6 修正 `zangli.js` 头部注释的两处错误日期
- [ ] P1-5 给 `getEclipse` 内的 `d` / `extraInfo` / `extraInfo2` 补 `var`

验收：写 6 个断言（5 个藏历新年锚点 + 2025-09-08 日月食），在 `TZ=Asia/Shanghai` / `UTC` / `America/New_York` 三个时区下各跑一遍，全绿。

### 期二 · 单一数据源 + 测试 + CI（1–2 周）

- [ ] 抽出 `src/data/special-days.js` 与 `src/data/eclipse.js` 作为唯一权威数据
- [ ] 主库改为「算法 + 数据」分离，小程序 `require` 同一份（小程序用 npm 构建或构建脚本拷贝）
- [ ] 删掉 `eclipse.js` 的内联副本
- [ ] 测试套件：
  - 5 个藏历新年锚点（2019/2023/2024/2025/2026）
  - 全量往返一致性（36560 天，需反向转换；可直接采用 v2 的 `solarFromZangli()`）
  - 三时区日月食一致性
  - 数据 diff 守卫：小程序与主库的表必须逐项相等
- [ ] GitHub Actions 跑测试；`miniprogram-ci` 做预览/上传
- [ ] 发 npm 包 + `index.d.ts`

### 期三 · 小程序能力（2–4 周）

- [ ] 采纳 v2 的核心库（已实测 36560 天往返 0 误差、四时区一致、数据与主库 0 diff），但合入前先做三件事：
  1. 删掉 v2 内联的数据拷贝，改为引用期二的统一数据文件（否则等于新增第四份拷贝）
  2. 统一 API 参数基准 —— v2 的 `solarFromZangli()` 中 `month` 是 0-based、`day` 是 1-based，而 `getZangli()` 返回的 `dayIndex` 是 0-based，极易踩坑
  3. 换成你自己的 appid
- [ ] `app.json` 启动页改为 `pages/index/index`（P1-2）
- [ ] 移除 `wx.cloud.init` 与 `pages/connect`、`page_module_sign_up`（P1-3/P1-4），省掉云开发费用
- [ ] 写入系统日历 `wx.addPhoneCalendar`（萨嘎达瓦、神变节、日月食提醒）
- [ ] 反向转换页（回应 issue #2）
- [ ] 朋友圈分享 `onShareTimeline`

### 期四 · 网站（1–2 月）

- [ ] `calendar/` 改为用核心库 + 构建脚本生成，补上 1951 年
- [ ] `index.html` 加 PWA（manifest + service worker）
- [ ] README 重写（数据出处、覆盖范围、流派声明 Phugpa、Tyme 致谢）
- [ ] 确认 CNAME 与 HTTPS 证书状态

### 期五 · 长期（持续）

- [ ] 引入 `@hnw/date-tibetan` 生成 1951–2050 推算结果，与历书数据逐日 diff → 一致部分作为 2050 后外延，不一致部分形成勘误表
- [ ] NASA Five Millennium Canon 扩充日月食数据至 2100 年（**必须先用期一修好的 UTC+8 逻辑重新生成日期键**）
- [ ] 藏文字体（`wx.loadFontFace` + 子集化）与中/藏/英 i18n（回应 issue #3）

---

## 4. Kimi 建议逐条对照

| Kimi 建议 | 结论 | 依据 |
|---|---|---|
| 双份数据漂移 | **修正** | 恢复后仅剩 1 条（P0-5），另新增「范围少 30 天」问题（P0-2） |
| 日月食时区 bug（归给小程序） | **修正** | 主库同样存在，两边都要改（P0-1） |
| 半天边界 | **采纳，但只剩主库** | 小程序备份版已修（P0-3） |
| 隐式全局变量 | **采纳** | P1-5，实测可触发报错 |
| 数字入参注释错误 | **采纳** | P1-6，且注释里的日期还自相矛盾 |
| 小程序工程残缺 | **已解决** | 备份已恢复；但发现新的死链 `search()`（P1-1） |
| `reportMonitor`/`checkLine`/`rem` 噪音 | **采纳** | P1-7 |
| 交付的 v2 工程 | **采纳（先改 3 处）** | 见期三 |
| ICP 备案 + 隐私指引 | **采纳，最高优先级** | 强制项，不处理可能清退 |
| 写入系统日历 | **采纳** | 日历类工具的杀手级接口，替代玩不转的长期订阅 |
| 朋友圈分享 | **采纳** | 低成本 |
| Skyline + Worklet 手势 | **推迟到三期后** | 先保证正确性和能上架，渲染层升级收益次之 |
| 藏文字体 / i18n | **采纳，排期五** | 需要字体子集化工作，依赖 issue #3 的真实需求 |
| 一次性订阅消息 | **同意 Kimi 的保留意见，不做** | 系统日历方案更优且零成本 |
| npm 包 / TS 声明 / CI | **采纳** | 期二 |
| date-tibetan 交叉校验 | **采纳** | 解决 2051 数据耗尽的唯一可持续路径 |
| NASA 日月食数据扩充 | **采纳** | 注意先用修复后的 UTC+8 逻辑重新生成日期键 |
| PWA | **采纳** | 期四 |
| 虚拟支付变现 | **搁置** | 与本项目公益/工具定位不符 |
| 「每日五要素」增值层 | **搁置** | 需时轮历专业支持，出错风险高于收益 |

### Kimi 未覆盖、本次新发现

- 小程序可查范围比主库少 30 天（P0-2）
- 小程序版已修复了主库仍存在的半天边界与食甚丢失（说明主库不是「最新最全」的那一份）
- `search()` 死链（P1-1）
- 小程序硬编码云环境、云开发已收费（P1-3）
- `pages/connect`、`page_module_sign_up` 未注册（P1-4）
- `wxapp/` 下的小程序是个**独立 git 仓库且从未配置 remote**——这才是它 7 年没同步的真正原因

---

## 5. 需要你决策

1. **启动页**：是否把 `app.json` 的 pages 顺序改为 `pages/index/index` 在前？
2. **去云开发**：是否移除 `wx.cloud.init` 与 `connect` / `page_module_sign_up` 两个页面？（省掉每月 19.9 元，且本程序不需要云）
3. **`project.config.json` 是否入库**：它含 appid `wxc1afb48b4d7e79ce`。appid 本身不保密（小程序码、分享链接里都带），入库能让 clone 后直接打开；介意的话改为忽略 + 提供模板文件。
4. **小程序仓库**：`wxapp/` 历史上是独立 git 仓库且无 remote。是并入 `stonelf/zangli`（当前做法），还是单独建仓库？
5. **v2 是否合入**：合入能一次性获得反向转换、今日高亮、写入系统日历，但会替换掉你 2021 年手改的那份（需要先确认备份版有没有 v2 缺失的改动）。

---

## 附：本次实测记录

- 环境：Node v22.22.2
- 锚点：2019/2023/2024/2025/2026 藏历新年 + 1951-01-08，主库与小程序均命中正月初一
- 时区：Asia/Shanghai、UTC、America/New_York 三时区各跑一遍
- 数据：主库与小程序 101 年逐项 diff → 1 条差异；`eclipseList` 495 条完全一致
- 范围：主库可查至 2051-02-10，小程序至 2051-01-11
- v2：36560 天（1951-01-08 ~ 2051-02-11）正向→反向往返 0 误差；四时区输出一致
